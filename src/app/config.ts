const apibaseUrl = "http://localhost:2628";
export const ApiEndpoints = {
  auth: `${apibaseUrl}/token`,
  user: `${apibaseUrl}/users`,
  category: `${apibaseUrl}/categories`,
  product: `${apibaseUrl}/products`,
  measureUnit: `${apibaseUrl}/measureunits`,
  Auth: `${apibaseUrl}/token`
} ;
